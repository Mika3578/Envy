{
  "name": "Envy (Linux dev checks)",
  "user": "ubuntu",
  "install": "sudo apt-get update && sudo apt-get install -y clang-format-18 clang-tidy cppcheck && npm --prefix Remote/tests ci --no-audit --no-fund"
}
